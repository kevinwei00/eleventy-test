<section class="page-section {{ classes }}">
  <div class="container">
    <div class="title-block">
      <h2>{{ title | safe }}</h2>
      <p class="lead">{{ text | safe }}</p>
    </div>
  </div>
</section>